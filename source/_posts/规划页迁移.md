---
title: 规划页迁移
date: 2026-08-14 12:00:00
categories: [FlyerLink	，规划页]
tags: [标签1, 标签2, 标签3]
description: 规划页 6 按钮迁移开发日志
cover: /img/banner.jpg
# sticky: 1   # 想置顶就取消这行注释（数字越大越靠前）
---

##  前言

规划页（Plan View）的 6 个功能按钮迁移：文件 / 起始点 / 设置航点 / 兴趣区域 / 图案 / 返航。这些按钮旧版在 Qt5 下用 ButtonStyle 实现，新版 Qt6 已弃用该机制，需要改造。

## 一、整体思路

6 个按钮 = 一个自定义控件（CustomPlanMenuButton）× 6 + 两个弹窗 + 交互逻辑：

```text
CustomPlanMenuButton（按钮样式/变黄/互斥）
  ├── "文件"按钮  → syncDropPanel 弹窗（创建计划/存储/设备）
  ├── "起始点"按钮
  ├── "设置航点"按钮
  ├── "兴趣区域"按钮
  ├── "图案"按钮  → patternDropPanel 弹窗（创建复杂图案）
  └── "返航"按钮
```
## 二、按钮控件迁移：CustomPlanMenuButton（点击变黄的秘密）

**文件：src/QmlControls/CustomPlanMenuButton.qml**

Qt5 → Qt6 的核心改造（这是最关键的）：
| 旧版（Qt5）	| 新版（Qt6）	| 原因 |
| --- | --- | --- |
| import QtQuick 2.3	| import QtQuick	| Qt6 去版本号 |
| import Controls.Styles 1.4	| 删掉	| ButtonStyle 已废弃 |
| style: ButtonStyle { } 包壳	| background: Rectangle { }	| 新版直接自定义背景 |
| style: ButtonStyle { label: Item {} }	| contentItem: Item {}	| 同上 |
| control.pressed	| _rootButton.pressed	| control 引用在 Qt6 无意义 |

**点击变黄的实现**（第 24、35 行）：
```qml
property color highlightColor: "#FFF291"   // 黄色高亮
// 背景色：按下或选中 → 黄色，否则 → 默认灰
color: (_rootButton.pressed || _rootButton.checked) ? highlightColor : backgroundColor
```
## 三、"文件"按钮 → syncDropPanel 弹窗

文件：src/PlanView/PlanView.qml 第 665-890 行

1. 点击弹出对话框：在文件中插入以下内容
```qml
CustomPlanMenuButton {
    text: qsTr("文件")
    onClicked: dropPanel.show(panelEdgeTopPoint, syncDropPanel, this)  // 弹窗弹出
}
```
2. 弹窗内容：syncDropPanel = 创建计划 / 存储 / 设备 三个分组选项。
3. 宽度、位置设置：弹窗用 panelEdgeTopPoint（按钮顶部边缘点）定位 + DropPanel 组件控制宽度，按钮在哪弹窗就跟在哪。





###  “文件”按钮点击弹出对话框

1、点击弹出对话框


2、点击颜色变黄

3、弹窗对话框的宽度设置、位置设置

4、汉化

5、倒三角常显

##  "图案" 按钮点击弹出对话框


<img width="424" height="447" alt="图片" src="https://github.com/user-attachments/assets/dd7e2897-dec1-438a-ba9c-07856975c4cb" />

正文文字，支持 **加粗**、*斜体*、~~删除线~~、`行内代码`。

##  规划页工具栏

添加了返回

1. 有序列表第一项
2. 有序列表第二项

- 无序列表
- 无序列表

#### 代码块（写代码必用，自动高亮）

```cpp
// C++ 示例
int main() {
    return 0;
}
```

```qml
// QML 示例
Rectangle {
    width: 100
    height: 100
    color: "red"
}
```

#### 引用

> 这是引用块，适合放重点提示或名言。

#### 图片

![图片说明文字](/img/xxx.jpg)

#### 链接

[链接文字](https://example.com)

#### 表格

| 列1 | 列2 | 列3 |
| --- | --- | --- |
| 内容 | 内容 | 内容 |

---

## 📌 写作流程（记住）

1. 复制这个文件的内容，改标题和正文
2. 保存为 `source/_posts/标题.md`（或运行 `hexo new post 标题` 自动生成）
3. 跑这一条命令更新上线：
   ```
   cd /c/Users/Administrator/myblog && NODE_OPTIONS= node node_modules/hexo/bin/hexo clean && NODE_OPTIONS= node node_modules/hexo/bin/hexo generate && NODE_OPTIONS= node node_modules/hexo/bin/hexo deploy
   ```

## 📌 front matter 字段说明

| 字段 | 作用 | 示例 |
| --- | --- | --- |
| title | 文章标题 | 我的第一篇学习笔记 |
| date | 发布日期（自动）| 2026-08-14 10:46:59 |
| categories | 分类（会进「分类」菜单）| QGC开发 |
| tags | 标签（会进「标签」菜单）| [侧边栏, QML] |
| description | 摘要 | 一段简介 |
| cover | 封面图路径 | /img/banner.jpg |
| sticky | 置顶（可选）| 1 |

> 💡 分类和标签是**自动聚合**的：写了 `categories: QGC开发`，点「分类」菜单就能看到「QGC开发」这个分类下所有文章。
